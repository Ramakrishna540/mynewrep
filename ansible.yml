---
- 
  hosts: group1
  tasks:
  - name: Install apache2 packages 
    yum:
      name: apache2
      state: present
  - name: ensure httpd is running
    service:
      name: apache2
      state: started
  
